# pandas_black_belt

**Skill:** Messy retail tables — `groupby` + `resample`, `merge_asof` for price books, SKU-location aggregates.

**RELEX relevance:** Real POS/ERP extracts are irregular; black-belt pandas is table stakes before any model.

**Run:**
```bash
python arena/pandas_black_belt/messy_retail_kungfu.py
```
