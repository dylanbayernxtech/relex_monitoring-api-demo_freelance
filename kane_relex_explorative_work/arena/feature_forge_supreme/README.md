# feature_forge_supreme

**Skill:** Calendar / lag / rolling feature forge with **leakage-aware** `shift(1)` rollings and time-based split.

**RELEX relevance:** Feature craft for demand models; leaking future into rollings is an instant interview fail.

**Run:**
```bash
python arena/feature_forge_supreme/leakage_aware_features.py
```
