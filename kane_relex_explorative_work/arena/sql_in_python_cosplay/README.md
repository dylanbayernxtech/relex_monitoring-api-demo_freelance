# sql_in_python_cosplay

**Skill:** SQL window cosplay in pandas — `cumcount`, `shift`, rolling sums, dense rank (ROW_NUMBER / LAG / ROWS PRECEDING).

**RELEX relevance:** Interview whiteboards love windows; Java/Kotlin backends exist, but DS often prototypes windows in pandas first.

**Run:**
```bash
python arena/sql_in_python_cosplay/window_cosplay.py
```
