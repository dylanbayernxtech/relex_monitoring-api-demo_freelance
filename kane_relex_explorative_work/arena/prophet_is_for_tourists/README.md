# prophet_is_for_tourists

**Skill:** Classical time series with **statsmodels** only — Holt-Winters ETS + light SARIMAX. No Prophet install required.

**RELEX relevance:** Interviewers still probe ETS/ARIMA intuition even when production leans ML / Vertex AI; show you can do both without tourist packages.

**Run:**
```bash
python arena/prophet_is_for_tourists/ets_arima_cosplay.py
```
