# sku_location_whisperer

**Skill:** Sparse **SKU × location** panel modeling — local means vs Ridge pooling across locations/SKUs.

**RELEX relevance:** Planning is SKU-location granularity; many cells are sparse. Pooling / hierarchy beats lonely per-series fits.

**Run:**
```bash
python arena/sku_location_whisperer/panel_pool.py
```
