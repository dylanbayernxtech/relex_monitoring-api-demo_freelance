# interview_warmachine

**Skill:** Timed gauntlet — runs every arena drill via subprocess, prints PASS/FAIL + wall clock.

**RELEX relevance:** Morning-of confidence: one command proves the whole pack is green before you walk into Helsinki energy (or the Zoom).

**Run:**
```bash
python arena/interview_warmachine/run_all.py
```
Aim: all drills PASS. Fix any red before the interview.
