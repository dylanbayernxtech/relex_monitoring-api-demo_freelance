# RELEX interview DS stack — CPU-only, scripts-first (no Jupyter required)
pandas>=2.1,<3
numpy>=1.26,<3
scikit-learn>=1.4,<2
matplotlib>=3.8,<4
scipy>=1.11,<2
statsmodels>=0.14,<1
plotly>=5.18,<6
# Optional — only if THEY ask for a notebook:
# jupyter>=1.0,<2
# ipykernel>=6.29,<7
# Optional heavy models (skip on failure):
# prophet
# lightgbm
