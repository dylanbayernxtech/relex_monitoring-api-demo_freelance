# Kane · RELEX explorative work

Branch: `kane_relex_explorative_work`

Interview-ready Python DS arena (13 arrogant drill packs) + RELEX company brief materials for Dylan Kane.

## Quick start
```powershell
cd kane_relex_explorative_work
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python warmup.py
python arena\interview_warmachine\run_all.py
```

## Arena packs
See `arena/README.md`.
