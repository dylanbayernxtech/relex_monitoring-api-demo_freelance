# Interview desk — locked setup

## PRIMARY (fullscreen for live audience)
**VS Code Insiders** — this folder only:

C:\Users\Dylan Kane\relex_recbktrees_freelance\relex_monitoring-api-demo_freelance.worktrees\kane-relex-explorative-work

Layout: editor ~70% | integrated terminal ~30%. One .py file at a time. Keep Agents/Copilot docked, not fullscreen.

Interpreter: C:\Users\Dylan Kane\.venvs\relex-ds\Scripts\python.exe

Morning smoke (~30s):
`powershell
cd "...\kane_relex_explorative_work"
.\run_arena.ps1
`

## AI surfaces (no paywall worry)
1. GitHub Copilot / Insiders Agents — free tokens; strongest model first
2. Claude Code — second coding agent on the repo
3. xPathfinderLabs + RELEX DS Primary / Kimi Fallback teammates

## Notebook
Kernel: **Python (relex-ds)**. Only if they require .ipynb. Default artifact = runnable .py (FINLAND-BAR.md).

## Opening impression
Start with kane_relex_explorative_work/interview_desk.ipynb open (kernel **Python (relex-ds)**). Still ship a runnable .py if that is the bar — notebook is the stage set.
